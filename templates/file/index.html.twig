<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
    </head>
    <body>
        {% block content %}
        {{form_start(form)}}
        <h3>Téléverser des fichiers pdf habililations et ccps</h3>
        <h4>les fichiers doivent avoir le même nom que libellé ccp ou titre habilitation</h4>
        <div>
            <div style="display:inline; border: 1px solid black">{{ form_widget(form.file)   }}</div>
            <div style="display:inline">                         {{ form_widget(form.upload) }}</div>
            <div>{{ message }}</div>
        </div>
        {{form_end(form)}}
        <div><a href="{{path('admin')}}">Admin</a></div>
        <div><a href="{{path('home') }}">Site</a></div>
        {% endblock %}
    </body>
</html>
